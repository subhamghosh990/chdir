1. Code is present in code directory.
2. There is test file to test the code in code dirctory. To run use the command 'go test -v .'
3. executable is already created. To run manual cases, use command '.\main.exe /aba /' from parent directory , where first arg is current path and second arg is requested new path
4. To create new executable, use command 'go build' from parent directory 